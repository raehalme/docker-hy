I use Java and Maven a lot in AWS for which I created the following image for building projects: https://hub.docker.com/r/aitiogroup/maven-aws

The image builds on the official Maven image so it's very simple. Just adds AWS CLI on top of Maven.
